using System;
using System.Windows.Controls;

namespace Hpl.Acm
{
    public class ConsoleLogs
    {
        //private void WriteToConsole(RichTextBox rtb, string message)
        //{
        //    Dispatcher?.Invoke(() => { rtb.AppendText(DateTime.Now.ToString("G") + ": " + message + "\r"); });
        //}
    }
}